# Environment variables for Render deployment
# Add your Groq API key here in Render dashboard

# Required for Gradio deployment
GRADIO_SERVER_NAME=0.0.0.0
GRADIO_SERVER_PORT=${PORT}

# Optional: Set to production for better performance
ENVIRONMENT=production
